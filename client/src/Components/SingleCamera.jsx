import { useState } from "react"

export const SingleCamera = ({brand, name, price, lens, features, img, sensor, video, files}) => {
    const [showInfo, setShowInfo] = useState(false)
    
    if (showInfo) {
    } else {
    }

    return(
        <div className="flex">

        </div>
    )
}